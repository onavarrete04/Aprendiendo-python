# vamos a ver los tipos de datos
print(type(8)) # es un numero entero y se representa como un int
print(type(10.5)) # es un dato decimal y se representa como float
print(type(1==1)) # boleano y se representa como bool
print(type('hola')) # es un string y se representa como str, los espacios tambien se ven en un estring
print(type(3+4j)) # es un dato complex que son numero complejos.
# VARIABLES una variable es un dato que se le asigna a un caracter
numero = 4
print(numero)
# con type se pregunta la variable
#cuando queremos presentar que no hay valor o tipo de valor
nada = None
# las variables no pueden contener un numero inicial ni un signo $, se caracteriza
# por tener ser un lenguaje sensitivo, por lo que las minusculas y las mayusculas hacen
# que sea una variable nueva, por lo tanto, es recomendable, utilizar buenos nombres de 
# variables que representen lo que estamos haciendo
#concadenacion significa la unión de dos string